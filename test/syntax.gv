# Gravel Syntax set

## Atom expressions
#

# Integer
1923

# Float
1965.34

# String
# Single line string
"string along for a line

# Multiline String
""
A string covering
several
lines
...
"
# Atoms can be followed by optional Kind annotation
1923 : Bit32U

## Function definition
#

# must have explicit parameter list, even if empty
fnc filter()

# ...and have kinded parameters
fnc filter(dom: Int node: Float)

# ...and handle optional trailing kinds
fnc filter(dom: Int node: Float) : Float

## Operater definition
#

# ...same as function definition
fnc ++=(dom: Int node: String) : String

## Function call
#
# explicit parameter delimits
filter(grabList())

# can be chained
filter(grabList()).filter2().filter3()

# can be chained with operater functions
filter(grabList()) + 44


## Operater call
#
# ...has one parameter even if empty
++=(5678)

# ...can have implicit parameter list,
#++= 49333 -93 /(45 * 3) 
++= 49333 -93 

# ...arguments can have optional kinds
++=(spiral(): Spiral)

# ...handles optional trailing kinds
++=(spiral(): Spiral) : String
